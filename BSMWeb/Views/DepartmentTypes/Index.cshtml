@model IEnumerable<BSMWeb.Models.DepartmentType>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/MasterPages/_Layout.cshtml";
}
@section PageStyle {
    @*You can add your custom style sheets for each page on this section.*@
    <link rel="stylesheet" href="http://cdn.datatables.net/plug-ins/725b2a2115b/integration/bootstrap/3/dataTables.bootstrap.css" />
}
@section PageJavaScript{
    @*You can add your custom JavaScript for each page on this section. This section will be added to the bottom of the page.*@
    <script type="text/javascript" src="../Content/Scripts/plugins/dataTables/jquery.dataTables.js"></script>
    <script type="text/javascript" src="http://cdn.datatables.net/plug-ins/725b2a2115b/integration/bootstrap/3/dataTables.bootstrap.js"></script>
    <script>
        $(document).ready(function () {
            $('#dataTables-example').dataTable({
                "order": [[0, "asc"]],
                "lengthMenu": [[100, 50, 25, 10, -1], [100, 50, 25, 10, "All"]]
            });
        });
    </script>
}
<section id="main-content">
    <section class="wrapper site-min-height">
        <h3>
            Department Types
        </h3>
        <hr />
        <div class="row">
            <div class="col-lg-12">
                <div class="panel panel-default">
                    <div class="panel-heading">
                        <h4 class="title">
                            Department Type List
                        </h4>
                    </div>
                    <div class="panel-body">
                        <div class="table-responsive">
                            <table class="table table-striped table-bordered table-hover" id="dataTables-example">
                                <thead>
                                    <tr>
                                        <th class="hidden">
                                            Director
                                        </th>
                                        <th>
                                            @Html.DisplayNameFor(model => model.DepartmentTypeName)
                                        </th>
                                    </tr>
                                </thead>
                                <tbody>
                                    @foreach (var item in Model)
                                    {
                                        <tr class="gradeU">
                                            <td>
                                                @Html.DisplayFor(modelItem => item.DepartmentTypeName)
                                            </td>
                                            <td>
                                                @Html.ActionLink("Create", "Create")|
                                                @Html.ActionLink("Edit", "Edit", new { id = item.DepartmentTypeId }) |
                                                @Html.ActionLink("Details", "Details", new { id = item.DepartmentTypeId }) 
                                                @*@Html.ActionLink("Delete", "Delete", new { id = item.DepartmentTypeId })*@
                                            </td>
                                        </tr>
                                    }
                                </tbody>
                            </table>
                        </div>
                        
                    </div>
                </div>
            </div>
        </div>
    </section>
</section>
